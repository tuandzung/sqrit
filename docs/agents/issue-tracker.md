# Issue tracker: GitHub

Issues and PRDs live in `tuandzung/sqrit` GitHub Issues. Use the `gh` CLI.

External PRs are a triage request surface. Include PRs from contributors and non-members; exclude owner, member, and collaborator PRs.

Prefer native GitHub sub-issues and blocking dependencies. Fall back to `## Parent` and `## Blocked by` body sections when unavailable.
