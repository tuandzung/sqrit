# Domain docs

This is a single-context repository.

Before exploring, read `CONTEXT.md` and the relevant records under `docs/adr/`. Use glossary terms in issue titles and acceptance criteria. Surface conflicts with existing ADRs explicitly.
